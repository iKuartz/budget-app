<div class="container">
  <%= form_with model: @entity, method:'post', 
      class:'form',
      url: entities_path(category_id: @category.id) do |form| %>
  <div class="flex main-bg-color color-white justify-center align-center justify-between height-40px">
    <%= link_to "🡄", entities_path(category_id: @category.id), method: :get, class:"back ml-10" %>
    <h2>Add Transaction</h2>
    <div class="actions color-white mr-10">
      <%= form.submit "Create"%>
    </div>
  </div>
    <div class="field">
      <%= form.text_field :name, autofocus: true, placeholder: "Name" %>
      <%= form.number_field :amount, autofocus: true, placeholder: "Amount" %>
      <%= form.select :selected_categories, current_user.categories.collect { |category| [category.name, category.id] }, {
        :multiple => true,
      }  %>
    </div>
  <% end %>
</div>